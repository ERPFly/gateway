---
import { cx } from "class-variance-authority";

type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<div class={cx("mx-auto max-w-7xl px-4 sm:px-6 lg:px-8", className)}>
  <slot />
</div>
